apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: mysql-pvc
  namespace: serverops

spec:
  accessModes:
    - ReadWriteOnce

  storageClassName: serverops-local

  resources:
    requests:
      storage: 10Gi
